# week6-spring-blog-api.zip
A backend RESTful API built with Java 17+ and Spring Boot 3.x for managing blog posts, categories, and comments. The project follows a layered architecture using Spring Web, Spring Data JPA, Hibernate, and an H2 in-memory database.
